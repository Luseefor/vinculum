"use client";

export default function AnimationTab() {
  return (
    <section className="rounded-lg border border-[var(--border-subtle)] bg-[var(--surface-overlay)]/30 p-3">
      <h3 className="text-[11px] font-semibold text-[var(--text-primary)]">Animation</h3>
      <p className="mt-1 text-[11px] text-[var(--text-tertiary)]">Parameter timeline and animation controls are queued for Slice 11.</p>
    </section>
  );
}
