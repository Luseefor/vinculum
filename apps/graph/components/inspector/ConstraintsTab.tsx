"use client";

export default function ConstraintsTab() {
  return (
    <section className="rounded-lg border border-[var(--border-subtle)] bg-[var(--surface-overlay)]/30 p-3">
      <h3 className="text-[11px] font-semibold text-[var(--text-primary)]">Constraints</h3>
      <p className="mt-1 text-[11px] text-[var(--text-tertiary)]">Constraint editor arrives in the constraints checkpoint.</p>
    </section>
  );
}
